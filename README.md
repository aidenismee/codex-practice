# Echo Starter

Basic Go API scaffold using the Echo framework.

## Run

```bash
go run ./cmd/server
```

## Endpoints

- `GET /` returns a friendly message.
- `GET /health` returns a JSON status payload.
